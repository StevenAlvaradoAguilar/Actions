name: Test

on: [push]

jobs: 
    test-build: 
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: ls
              uses: ./.github/actions/ls