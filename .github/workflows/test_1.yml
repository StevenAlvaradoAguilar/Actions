on: [push]

jobs: 
    hola: 
        runs-on: ubuntu-latest

        steps:
            - name: hola Mundo
              run: echo "hola mundo"

            - name: touch file
              run: touch hola.txt

            - name: Set text
              run: echo "hola mundo" >> hola.txt

            - name: Read file
              run: cat hola.txt

    others: 
        runs-on: ubuntu-latest

        steps:
            - name: LS
              run: ls -al